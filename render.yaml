services:
  - type: web
    name: agenda-vanessa-rorig
    runtime: static
    buildCommand: yarn install --ignore-engines && yarn export:web
    staticPublishPath: ./dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
